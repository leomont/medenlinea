@model IEnumerable<Medinlinea.Models.Curriculums>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ImagenCV)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionCV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ImagenCV)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionCV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCV }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCV }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCV })
        </td>
    </tr>
}

</table>
